<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.TeamProject.mapper.SubscriptionMapper">
	<!-- 메인용: 최대 4개 -->
    <select id="selectSubscriptionPlanMainList"
            parameterType="hashmap"
            resultType="hashmap">
        SELECT *
        FROM (
            SELECT
                PLAN_ID      AS "planId",
                PLAN_NAME    AS "planName",
                SHORT_DESC   AS "shortDesc",
                PERIOD_TYPE  AS "periodType",
                BASE_PRICE   AS "price",
                THUMBNAIL_URL    AS "imageUrl"
            FROM SUBSCRIPTION_PLAN
            WHERE IS_ACTIVE = 'Y'
            ORDER BY
                DISPLAY_ORDER NULLS LAST,
                PLAN_ID DESC
        )
        WHERE ROWNUM &lt;= 4
    </select>

    <!-- 리스트 페이지용: 전체 -->
    <select id="selectSubscriptionPlanList"
            parameterType="hashmap"
            resultType="hashmap">
        SELECT
            PLAN_ID      AS "planId",
            PLAN_NAME    AS "planName",
            SHORT_DESC   AS "shortDesc",
            PERIOD_TYPE  AS "periodType",
            BASE_PRICE   AS "price",
            THUMBNAIL_URL    AS "imageUrl"
        FROM SUBSCRIPTION_PLAN
        WHERE IS_ACTIVE = 'Y'
        ORDER BY
            DISPLAY_ORDER NULLS LAST,
            PLAN_ID DESC
    </select>

    <!-- 상세용: 1건 -->
    <select id="selectSubscriptionPlanDetail"
            parameterType="hashmap"
            resultType="hashmap">
        SELECT
            PLAN_ID      AS "planId",
            PLAN_NAME    AS "planName",
            SHORT_DESC   AS "shortDesc",
            DESCRIPTION  AS "description",
            PERIOD_TYPE  AS "periodType",
            BASE_PRICE   AS "price",
            THUMBNAIL_URL    AS "imageUrl"
        FROM SUBSCRIPTION_PLAN
        WHERE PLAN_ID = #{planId}
    </select>
    
    <insert id="insertSubscription" parameterType="hashmap">
	  <selectKey keyProperty="subscriptionId" resultType="int" order="BEFORE">
	    SELECT SUBSCRIPTION_SEQ.NEXTVAL FROM DUAL
	  </selectKey>
	
	  INSERT INTO SUBSCRIPTION (
	      SUBSCRIPTION_ID,
	      PLAN_ID,
	      USER_ID,
	      STATUS,
	      START_DATE,
	      NEXT_BILLING_DATE,
	      LAST_ORDER_NO,
	      CDATETIME,
	      UDATETIME
	  ) VALUES (
	      #{subscriptionId},
	      #{planId},
	      #{userId},
	      NVL(#{status}, 'ACTIVE'),
	      SYSDATE,
	      (
	        SELECT CASE PERIOD_TYPE
	                 WHEN 'WEEKLY'   THEN SYSDATE + 7
	                 WHEN 'BIWEEKLY' THEN SYSDATE + 14
	                 WHEN 'MONTHLY'  THEN ADD_MONTHS(SYSDATE, 1)
	                 ELSE NULL
	               END
	        FROM SUBSCRIPTION_PLAN
	        WHERE PLAN_ID = #{planId}
	      ),
	      #{orderNo},
	      SYSDATE,
	      SYSDATE
	  )
	</insert>
	
	<!-- 결제 예정일 3일 전인 구독 목록 조회 -->
	<select id="selectUpcomingBillingList" resultType="hashmap">
	    SELECT
	        USER_ID,
	        PLAN_ID,
	        (SELECT PLAN_NAME FROM SUBSCRIPTION_PLAN WHERE PLAN_ID = S.PLAN_ID) AS PLAN_NAME
	    FROM SUBSCRIPTION S
	    WHERE STATUS = 'ACTIVE'
	      AND TO_CHAR(NEXT_BILLING_DATE, 'YYYY-MM-DD') = TO_CHAR(SYSDATE + 3, 'YYYY-MM-DD')
	</select>

</mapper>